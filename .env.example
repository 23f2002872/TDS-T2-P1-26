TELEGRAM_BOT_TOKEN=
AIPIPE_TOKEN=
LOG_URL=https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/main/run.jsonl
MODEL_NAME=gpt-4.1-mini
# Optional: only needed to auto-push run.jsonl back to GitHub (e.g. when hosted).
# Leave unset for local dev — the bot just logs locally without these.
GITHUB_REPO=YOUR_USERNAME/YOUR_REPO
GITHUB_TOKEN=
